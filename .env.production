# Live data goes through the Cloudflare Worker proxy (worker/), which holds the
# OpenF1 paid login as secrets, refreshes the hourly token, and caches responses.
# This URL is public and safe to commit — the credentials live in Cloudflare, not here.
VITE_OPENF1_BASE_URL=https://f1dash-openf1.wattcm.workers.dev/v1
