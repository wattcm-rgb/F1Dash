# Self-hosted OpenF1 API, exposed over HTTPS via a Cloudflare quick tunnel.
# NOTE: trycloudflare URLs are EPHEMERAL — they change whenever the tunnel
# (or the VPS) restarts. Update this value and redeploy when that happens.
VITE_OPENF1_BASE_URL=https://approach-investor-expression-classified.trycloudflare.com/v1
